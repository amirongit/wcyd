services:
  redis-main:
    image: focker.ir/redis:7.2.5-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    ports:
      - '6379:6379'
    networks:
      - wcyd


volumes:
  redis-data:

networks:
  wcyd:
